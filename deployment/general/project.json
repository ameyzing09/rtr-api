{
  "name": "general",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "infrastructure",
  "sourceRoot": "deployment/general",
  "targets": {
    "deploy:dev": {
      "executor": "@nx/run-commands",
      "options": {
        "command": "terraform apply -auto-approve",
        "cwd": "deployment/general/environments/dev"
      }
    },
    "deploy:ppe": {
      "executor": "@nx/run-commands",
      "options": {
        "command": "terraform apply -auto-approve",
        "cwd": "deployment/general/environments/ppe"
      }
    },
    "deploy:prod": {
      "executor": "@nx/run-commands",
      "options": {
        "command": "terraform apply -auto-approve",
        "cwd": "deployment/general/environments/prod"
      }
    },
    "plan:dev": {
      "executor": "@nx/run-commands",
      "options": {
        "command": "terraform plan",
        "cwd": "deployment/general/environments/dev"
      }
    }
  },
  "tags": ["infrastructure", "aws", "general"]
}
